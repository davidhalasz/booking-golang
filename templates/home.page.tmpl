{{template "base" .}}

{{define "content"}}
    <div class="container mx-auto py-10">
        <div class="flex flex-col">
            <h1 class="text-2xl font-bold text-blue-500">Hello HOME Page</h1>
            <p class="text-md font-bold text-blue-500">Home content</p>
        </div>
    </div>
{{end}}